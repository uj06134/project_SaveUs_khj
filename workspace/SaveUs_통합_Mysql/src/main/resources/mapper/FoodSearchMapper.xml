<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.Ex02.mapper.FoodSearchMapper">

    <select id="searchFood" parameterType="string"
            resultType="com.example.Ex02.dto.FoodNutritionDto">
        SELECT
        FOOD_ID       AS foodId,
        FOOD_NAME     AS foodName,
        CATEGORY      AS category,
        CALORIES_KCAL AS caloriesKcal,
        CARBS_G       AS carbsG,
        PROTEIN_G     AS proteinG,
        FAT_G         AS fatG,
        SUGAR_G       AS sugarG,
        FIBER_G       AS fiberG,
        CALCIUM_MG    AS calciumMg,
        SODIUM_MG     AS sodiumMg
        FROM FOOD_NUTRITION
        WHERE LOWER(FOOD_NAME) LIKE CONCAT('%', LOWER(#{keyword}), '%')
        ORDER BY FOOD_NAME
    </select>

    <select id="searchFoodNames" parameterType="string" resultType="string">
        SELECT
        FOOD_NAME
        FROM FOOD_NUTRITION
        WHERE LOWER(FOOD_NAME) LIKE CONCAT('%', LOWER(#{keyword}), '%')
        ORDER BY FOOD_NAME
        LIMIT 10
    </select>

</mapper>
